# 2026-05-21 工作日志

## 战术甘特图级重构 (v4.0)

### Python 管线改造
- `src/main.py`: 新增 `annotate_global_ids()` 函数，在 `assemble_week_schedule` 后自动按时间排序为全周活动分配 `global_id` (1-9)
- 重新运行管线生成 `output/week_schedule.json`，确认所有活动都携带 `global_id` 字段
- Python 环境：已配置 `C:\Users\yliua\.workbuddy\binaries\python\envs\default` venv，安装了 requests

### index.html 全面重构
**三区 global_id 显性展示：**
- A区：每张海报卡片左上角蓝色盾牌形徽章，大写编号
- B区：精确图钉内旋转红色数字、模糊热力圈蓝色圆形数字标记
- C区：甘特块内小圆形编号

**C区横向战术甘特图：**
- C1 轴（底部）：08:00-24:00 渐变时间轴，黄→红→蓝→深黑蓝 CSS gradient
- C2/C3 轨：活动按交替规则摆放（idx%2），避免视觉重叠
- 方块左边缘对齐开始时间，宽度=活动时长
- 冲突活动带脉冲动画标记

**筛选引擎：**
- A区顶部 全局/周五/周六/周日 Tab 切换
- 同步过滤 A区海报墙、B区地图、C区甘特图

**跨区联动高亮：**
- 点击任意区元素 → 通过 global_id 查找对应项
- A区：scrollIntoView + 发光脉冲边框
- B区：flyTo 坐标 + 弹出 popup
- C区：甘特块发光高亮 + 水平滚动到位
- 悬停时软高亮（不触发flyTo）

### 数据流
- 9个活动，global_id 1-9 按时间顺序排列
- 周五2场、周六4场（含1组冲突）、周日3场（含1组冲突）

---

## 战术指挥舱 v5.0 重构 (同日第二次迭代)

### A区 Master-Detail 布局
- 全局模式：A区100%宽度海报Grid
- 分日模式：左右50%切割，左Detail详情面板 + 右List单列卡片
- Detail面板展示全部情报：基础信息Grid、性别比例、门槛、环节流程、精华文本(大字体)、性价比评估
- 点击卡片/地图/甘特块均可联动到Detail面板

### C区二次切割 — C4 面板
- C4面板占C区上66.6%，甘特图压缩至下33.3%
- C4-左(90%宽)：冲突对比内嵌面板，点击冲突活动渲染对比表格；无冲突显示占位符
- C4-右(10%宽)：战术气象与限行面板
  - Mock数据按日配置：周五(N35°22'13.9"N / 116°39'03.5"E)22~32℃限行4和9、周六(多云20~30℃不限行)、周日(晴转多云21~31℃不限行)
  - 垂直排列：日标签→天气图标→描述→温度→限行信息
  - 分日模式常驻，全局模式显示周末总览

### C2/C3 甘特块信息降噪 (v5.0)
- 三行垂直布局：时间(13px/800粗体/最大)→名称(10px)→主办方|¥价格|人数(7.5px/一行)
- overflow:hidden + text-overflow:ellipsis 防止溢出
- global_id小圆放在左侧独立列

### 联动升级
- Tab切换同步A区(布局模式切换+筛选)、B区(地图坐标)、C区(甘特图+C4天气+C4冲突重置)
- 移除弹窗式冲突面板，改为C4内嵌渲染
- B区点击marker和C区点击甘特块均联动A区Detail面板
- 全局模式下Detail隐藏（使用CSS display:none）

### 技术细节
- 移除了fixed overlay冲突面板 HTML/JS/CSS
- C1轴高度压缩至30px (从46px)
- 气象限行数据通过`WEATHER_DATA`对象按日Mock

---

## 战术指挥舱 v5.1 — 无损还原 + 自动部署 (同日第三次迭代)

### 底层数据无损还原
- **ima_importer.py**: 废除 `raw_text` 截断逻辑(`[:500]`)，新增 `extract_full_raw_text()` 返回完整正文（保留所有换行符），新增 `extract_all_images()` 提取 Markdown(`![img](url)`) 和 HTML(`<img src>`) 图片链接存入 `images` 数组
- **data_pipeline.py**: `parse_activity()` 新增 `full_raw_text` 和 `images` 字段透传至最终 `week_schedule.json`
- **sample_data.py**: 为所有9条 SAMPLE_EVENTS 同步补齐 `full_raw_text` 和 `images` 字段
- 管线运行验证通过：`full_raw_text=True, images=True` 已穿透至最终 JSON

### A区Detail面板全量渲染
- 新增 `.ad-full-text` CSS：`white-space:pre-wrap` + `max-height:360px` + `overflow-y:auto` 独立滚动区，左侧金色竖线标记
- 新增 `.ad-images-grid` CSS：响应式图片网格，hover放大效果，加载失败自动隐藏
- `renderDetailPanel()`: 优先渲染 `full_raw_text`（完整情报），回落 `essence_text`（精华情报）；底部追加图片画廊
- IMA 链接确认 `target="_blank"` 正常，点击不冒泡

### 全自动 DevOps 部署 (auto_deploy.bat)
- Step 1: 激活虚拟环境 `C:\Users\yliua\.workbuddy\binaries\python\envs\default\` → 执行 `python src/main.py`
- Step 2: `git init`(幂等) → `git branch -M main` → `git remote add/set-url`(幂等) → `git add .` → `git commit` → `git push -f -u origin main`
- Step 3: 推送成功后 `shutdown /s /t 3600`（预留1小时检查窗口）
- 仓库地址: `https://github.com/independent-all/workbuddy--.git`

---

## 战术指挥舱 v6.0 — 真实数据接入 + 移动端响应式装甲 (同日第四次迭代)

### 废弃假数据，接入真实笔记
- **main.py**: 彻底移除 `from sample_data import SAMPLE_EVENTS`，改为导入 `ima_importer.scan_and_import`
- 新增 `load_real_events()` 函数：从项目根 `ima_notes/` 加载 `.md`/`.txt` 真实笔记
- **容错机制**：
  - `ima_notes` 不存在 → 自动创建空文件夹 → 黄色提示 → `sys.exit(0)`
  - 文件夹存在但无可解析文件 → 红色错误提示 → `sys.exit(0)`
- 当前 `ima_notes/` 有3个真实笔记，管线跑通生成3条活动（#01~#03）

### 移动端响应式装甲 (.mobile-mode)
- **header新增切换按钮**：📱 手机/PC视图切换 (`.view-toggle`)，toggle `<body>` 的 `mobile-mode` class
- **自动检测**：`setupViewToggle()` 在 `init()` 时检测 `window.innerWidth < 768`，自动激活手机模式
- **用户可手动切换**：按钮持久生效，不依赖自动检测
- **`.mobile-mode` CSS 专属规则（仅在body带class时激活，PC端零影响）**：
  - 主布局 `flex-direction: column` 垂直堆叠 A / (B+C)
  - A区 100%宽；分日模式 Detail + List 上下堆叠（原左右50%）
  - B区地图 100%宽 + 固定 `380px` 高
  - C区 C4冲突/气象面板 `flex-direction: column` 上下堆叠，气象面板改为横向排列
  - 甘特图 `.gantt-container` 设 `min-width: 800px` + 外层 `.gantt-scroll` 设 `overflow-x: auto`，手指左右滑动
- **地图刷新**：切换视图后 `setTimeout(map.invalidateSize, 300)` 确保 Leaflet 自适应
- **resize 监听增强**：窗口变化时自动调整 `overflowY`，小屏保持可滚动

### IMA 链接优雅降级 (Task 3)
- `renderPosterCard()`: `ima_source_url` 为空 → 显示置灰 `📎 无原始链接` (opacity:0.4, 不可点击)
- `ima_source_url` 存在 → 正常 `<a target="_blank" rel="noopener">` 链接
- A区Detail面板同逻辑保持（本就做了 `act.ima_source_url ?` 条件判断）
